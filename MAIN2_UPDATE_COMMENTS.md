# Main2: краткие комментарии по изменениям после merge с MAIN

База: `ead28a5` — merge `MAIN` в `Main2` от 2026-06-02.

## 2026-06-03
- Добавлена/доработана система уведомлений и команд рабочих таблиц.
- Подготовлена передача действий через `TWorkTable`.

## 2026-06-04
- Исправлены операции copy/cut в `DeviceSelect`.
- Убраны лишние автоподстановки выбора строки после фильтрации дерева.
- Исправлены дубли при выборе приборов.

## 2026-06-05
- Серия правок `TypeSelect` по пустым веткам дерева.
- Исправлена работа с пустыми/нестандартными категориями.
- Улучшена загрузка данных в выборе типов.

## 2026-06-08
- Добавлено поле `Temp` для типов и приборов: модель, БД, редакторы, списки.
- Добавлена колонка температуры в `TypeSelect` и `DeviceSelect`.
- Доработана привязка прибора к репозиторию типа.
- Добавлено меню пересчёта единиц расхода в таблице диаметров.
- Старт/стоп измерений переведён на команды рабочей таблицы.

## 2026-06-09
- Доработана синхронизация дерева `TypeSelect` после редактирования типа.
- После редактирования выбирается актуальный тип, чтобы фильтр грида оставался корректным.


## 2026-06-09 — дополнение после последней записи
- Контекстные меню рабочих таблиц перенесены с панелей инструментов в popup-меню самих гридов.
- Старые пункты меню сгруппированы внутри popup-меню по смыслу: добавление, изменение, удаление, прочее и управление колонками.
- Исправлены некорректно сохранённые FMX-строки пунктов меню после переноса.
- Визуальные размеры вкладок и областей гридов в `frmMainTable.fmx` приведены к рабочему состоянию после правок формы.
- Запуск мониторинга из UI оставлен через `FActiveWorkTable.StartMonitor` с записью действия в протокол.

## 2026-06-10
- При открытии выбора приборов в главной таблице добавлена переинициализация каналов рабочей таблицы перед удалением каналов по удалённым UUID.
- В `TFlowMeter.SetDeviceUUID` UUID теперь нормализуется через `Trim`; если уже привязанный прибор имеет другой UUID, ссылка на прибор сбрасывается для последующего корректного поиска/создания.
- `TFlowMeter.Init` доработан: ищет прибор по UUID через `DataManager`, создаёт/назначает прибор при необходимости, сбрасывает `ResultValue`, определяет категорию расходомера из прибора и переинициализирует значения.
- В `TWorkTable` добавлен публичный метод `InitChannels`, который проходит по каналам приборов и переинициализирует связанные `FlowMeter`.
- При подготовке измерения и при создании приборов для рабочей таблицы добавлена страховка: если список каналов пустой, создаётся канал по умолчанию.
- При сборе точек измерения добавлена проверка отсутствующего/пустого прибора канала и попытка создать прибор через `TDeviceCreationService.EnsureDeviceForChannel`.
- В редакторе прибора подписи нормального и переходного расхода теперь дополняются текущей размерностью прибора.

## Коротко по рискам
- `TypeSelect`: внимательно проверить фильтр дерева после изменения модификации последней строки.
- `DeviceSelect`: проверить cut/delete и отсутствие пустых веток.
- Температура: проверить сохранение/загрузку старых БД после добавления `Temp`.
- Диаметры: проверить пересчёт единиц только для нужных строк.

## 2026-06-11
- `frmWorkTableProperties` переведён на единый список свойств через `TTreeView`: общие свойства рабочего стола и группы `Давление`, `Температура`, `Расход`, `Жидкость` отображаются раскрывающимися разделами вместо панели с вкладками.
- В свойства рабочего стола добавлен заголовок колонок `Свойство` / `Значение` и разделительная линия, чтобы форма выглядела как остальные инспекторы свойств.
- Вызов `WorkTableManager.ActiveWorkTable.InitChannels` перенесён из UI-обработчиков `frmMainTable` в `TDeviceRepository.Load` после проверки схемы БД; прямые дублирующие вызовы при выборе/редактировании прибора удалены.
- Инициализация каналов при загрузке репозитория выполняется только при наличии `WorkTableManager` и активного рабочего стола.

## 2026-07-22 — диагностика графиков расхода
- FlowGraphRunState: RunActive определяется по активному `TMeasurementRun` и `Stage` не в `msNone/msDone`; NewRunStarted фиксируется по переходу `FLastGraphRunActive=False -> True`, а не только по смене ссылки объекта.
- FlowGraphReadiness: `FGraphChannelsReady=True`, когда созданы `FFlowGraphHistory`, логические серии текущего WorkTable и синхронизированные `TLineSeries`; при первом запуске MeasurementRun readiness принудительно доводится через `RefreshFlowGraphChannels` до добавления первого Sample.
- FlowGraphSample: сбор начинается сразу после определения `CurrentPoint` и готовности каналов; значения `0` сохраняются как валидные Samples, отбрасываются только отсутствующие каналы/приборы/значения и NaN/Infinity на рендере.
- FlowGraphRender: визуальные `TLineSeries` очищаются только внутри рендера и заполняются Samples текущего сегмента/окна; история предыдущих точек остаётся в runtime `Samples`.
- FlowGraphLimits: ErrorSource=TDevicePoint.Error; TargetLS=CurrentPoint.Q; PointErrorPercent=Abs(CurrentPoint.Error); LowerLS/UpperLS считаются как TargetLS ± Abs(TargetLS)*PointErrorPercent/100.0; `Point.FlowAccuracy` используется MeasurementRun для стабилизации, но не графическими линиями допуска.

## 2026-07-23 — Fix device stability target after PR #1807
- Причина ошибки PR #1807: `IsDevicesStable` использовал устаревший `Channel.FlowMeter.ValueFlow.StabilitySettings.TargetValue` как целевой расход поверяемого прибора, из-за чего точка около 16 м³/ч сравнивалась со старой целью 10 м³/ч и стадия `msWaitStable` могла не завершаться.
- Старый неправильный источник Target (`StabilitySettings.TargetValue`) исключён из расчёта стабильности поверяемых каналов.
- Новый источник Target для автоматической проливки — runtime-расход текущей точки `CurrentPoint.Q`; это соответствует значению, которое `SetupPoint` передаёт в `DoFlowRateSet`.
- Удалён запрещённый fallback `Target=Actual`: при отсутствии валидной цели канал считается нестабильным, а диагностика содержит `TargetNotAssigned`.
- Границы стабильности поверяемого прибора теперь рассчитываются по погрешности ближайшей включённой точки прибора, найденной по абсолютному `Q` в л/с, а не по доле `FlowRate` или настройкам `StabilitySettings`.
- Логика Auto исправлена: `RequireAutoStabilization=True` больше не подменяет стабильность значением `True`; измерение продолжается только после фактического `DevicesStable=True`.
- Таймер выдержки сбрасывается при потере стабильности, а при числовой Pause заново ждёт полный период после восстановления стабильности.
- Таймаут ожидания стабильности приборов теперь повторяет `SetupPoint` до лимита попыток и в automatic-режиме переводит ошибочную точку дальше, не оставляя бесконечный цикл `msWaitStable`.
- Проверка по текущему сценарию: для `CurrentPoint.Q=4,452778` л/с Target берётся из текущей точки, значение `2,777778` л/с из старых настроек больше не используется, а диагностическая запись `DeviceChannelStability` показывает выбранную точку прибора, её Q, погрешность и итоговую причину стабильности/нестабильности.
